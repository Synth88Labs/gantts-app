node_modules/
npm-debug.log*
.DS_Store
Thumbs.db

# deploy artifacts (regenerate with: node scripts/build-deploy.js)
deploy/
deploy-gantts-app.zip
.zip-bundle.ps1

# local tool state
.claude/settings.local.json

# Generated download artefacts (~9 MB of .xlsx/.pptx binaries).
# Rebuild with: npm run gen:templates
# Kept out of git because binaries diff poorly and every regeneration
# would store a whole new copy in history forever.
templates/files/

# FTP credentials — never commit
.env
